services:
  - type: web
    name: ai-pm-busm130
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ai_pm_app.backend.app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
